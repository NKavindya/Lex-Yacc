// Covers funcDef/funcBody, varDeclOrStmtList, localVarDecl, all statement forms (assign/IF/WHILE/READ/WRITE/RETURN/functionCall), statBlock variants ({} / statement / ε), fParams/aParams lists, statementList ε.

class A {
  public func foo(a : integer) => integer;
  private attribute v : integer;
  public func sum(a : integer, b : integer) => integer;
};

implement A {
  func foo(a : integer) => integer {
    local x : integer;
    local y : float;
    x := a + 1;
    if (x > 0) then { write(x); } else { read(x); };
    while (x < 3) { x := x + 1; } ;
    while (x < 2) x := x + 1;;     /* statBlock -> statement (double ;) */
    while (x < 0) ;                /* statBlock -> epsilon */
    x := sum(x, a);
    return (x);
  }

  func sum(a : integer, b : integer) => integer {
    return (a + b);
  }

  func bar() => integer {
    local z : integer;
    if (z == 1) then { } else { }; /* statementList -> epsilon */
    return (0);
  }
}

