// function with multiple local variables
func multipleLocals(a : integer, b : integer) -> integer {
    local x : integer;
    local y : integer;
    local z : integer;
    local result : integer;
    x := a + 1;
    y := b + 2;
    z := x + y;
    result := z * 2;
    return(result);
}

