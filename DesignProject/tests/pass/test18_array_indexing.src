// Focus: Array indexing
// Grammar: variable -> {{idnest}} id {{indice}}, indice -> [ arithExpr ], indiceList -> indice indiceList | epsilon

func test() => integer {
  local arr : integer[5][3];
  local x : integer;
  x := arr[0];
  x := arr[1][2];
  arr[0] := 10;
  arr[1][2] := 20;
  return (x);
}

