// Focus: Read and Write statements
// Grammar: statement -> READ LPAREN variable RPAREN ; | WRITE LPAREN expr RPAREN ;

func test() => integer {
  local x : integer;
  read(x);
  write(x);
  write(42);
  write(x + 1);
  return (x);
}

